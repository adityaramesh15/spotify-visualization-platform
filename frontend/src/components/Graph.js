import React from 'react';

const Graph = () => {
    return (
        <div>
            <h1>Graph</h1>
            {/* Add your graph component here */}
        </div>
    );
};

export default Graph;